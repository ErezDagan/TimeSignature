# Backend API base URL (no trailing slash)
# Local dev: leave empty (proxied via Vite) or point to local backend
# Production: set to your Railway backend URL
VITE_API_URL=https://your-backend.railway.app
